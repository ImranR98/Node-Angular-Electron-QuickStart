{
  "name": "naeq",
  "version": "1.0.0",
  "description": "",
  "main": "dist/main.js",
  "scripts": {
    "start": "node ./dist/main.js",
    "tsc": "tsc",
    "dev": "ts-node-dev --respawn --poll ./src/main.ts",
    "electron": "electron .",
    "build": "tsc; npm run build --prefix ./frontend; electron-packager . --out=build --platform=win32; electron-packager . --out=build --platform=darwin; electron-packager . --out=build --platform=linux"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^10.1.5",
    "electron-packager": "^15.1.0",
    "ts-node-dev": "^1.0.0",
    "typescript": "^4.0.5"
  },
  "dependencies": {
    "@types/express": "^4.17.8",
    "express": "^4.17.1"
  }
}
